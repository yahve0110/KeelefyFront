export interface Dictionary {
  general: {
    nextBtn: string;
  };
  nav?: {
    lessons?: string;
    dictionary?: string;
    profile?: string;
    settings?: string;
    chat?: string;
  };
  products?: {
    cart?: string;
  };
}